# Dependencias de la API (servidor). No incluye nada de la interfaz grafica:
# los modulos models/ son codigo puro y no dependen de Qt.
fastapi>=0.115
uvicorn[standard]>=0.32
psycopg2-binary>=2.9.0
reportlab>=4.0
bcrypt==4.0.1
requests>=2.31.0
